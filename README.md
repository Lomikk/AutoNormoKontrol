# Проверяемый шаблон курсовой работы ЮУрГУ

Профиль для ВШЭУ, кафедры «Цифровая экономика и информационные технологии»,
направления 09.03.02. СТО ЮУрГУ 21–2008 используется как выбранная базовая
спецификация, но его формальная применимость к техническому направлению и
актуальные локальные требования кафедры пока требуют подтверждения.

Это не обещание «ИИ не ошибается». Проект разделяет требования на четыре типа:

- измеримые правила, которые проверяют код, LaTeX и PDF-postflight;
- смысловые правила, которые ИИ обязан проверить с доказательствами;
- внешние факты и решения, которые вправе подтвердить только человек;
- неприменимые к курсовой пункты ВКР, которые остаются видимыми в реестре.

Подробные ответы о Draft/Strict, тестах, `content-hash`, смысловом аудите и
доказательствах собраны в [`docs/FAQ.md`](docs/FAQ.md). Работа без локального
агента — через браузерный чат, ручной diff или Aider — описана в
[`docs/WEB_CHAT_WORKFLOW.md`](docs/WEB_CHAT_WORKFLOW.md).
Отложенная эволюция от одного профиля к разным типам документов и кафедрам
зафиксирована в [`docs/ROADMAP.md`](docs/ROADMAP.md).
Практические ответы пользователя и выведенные из них архитектурные ограничения
собраны в [`docs/PRODUCT_DECISIONS.md`](docs/PRODUCT_DECISIONS.md); этот журнал
отделяет желаемое поведение продукта от возможного технического способа.

## Быстрый запуск через CLI

Из корня движка запусти:

```powershell
.\AutoNormoKontrol.cmd
```

Откроется меню управления движком: создание работы, проверка движка,
диагностика зависимостей и установка. Корень не является документом и не
поддерживает `draft`, `strict`, `status`, `open`, `export` и `archive`.
Вывод native-инструментов декодируется в месте их запуска, поэтому CLI не меняет
глобальную кодовую страницу консоли.

Те же действия можно выполнять без меню. Канонический полный список команд и
их точное назначение находится в
[`docs/PROJECT_STRUCTURE.md`](docs/PROJECT_STRUCTURE.md); здесь не поддерживается
второй независимый список.

CLI запускается через небольшой `.cmd`-файл, поэтому пользователю не нужно
менять системную PowerShell Execution Policy. Все операции выполняются локально
на компьютере пользователя. CLI не присваивает статусы аудита или внешней
приёмки и не обходит блокеры `Strict`.

### Новая курсовая: полный локальный цикл

Из центрального каталога движка создай отдельную рабочую папку:

```powershell
.\AutoNormoKontrol.cmd list-profiles
.\AutoNormoKontrol.cmd new "Моя курсовая"
cd ".\Workspaces\Моя курсовая"
.\gemini.cmd
```

Без локального агента документ собирается той же публичной командой:

```powershell
.\AutoNormoKontrol.cmd draft
```

Внутри workspace лежат только данные одной работы, тонкие launchers и начальный
вариант системной инструкции выбранного профиля
`guide/profile-system-prompt.md`. Реализация движка, полный пакет профиля и
нормативные источники туда не копируются. Локальный prompt имеет одинаковое имя
во всех работах, но разное исходное содержимое для разных профилей. Пользователь
может адаптировать его для конкретной работы, не меняя центральный профиль.
Порядок Markdown-глав явно хранится в `project.yaml`, поэтому добавление главы не
требует правки профиля или сканирования каталога.
Команды документа запускаются только из этой папки; наличие `project.yaml`
является обязательной границей.

После успешного Draft последний проверенный снимок можно опубликовать и при
необходимости заархивировать:

```powershell
.\AutoNormoKontrol.cmd export
.\AutoNormoKontrol.cmd archive sent-to-teacher
```

`export` создаёт постоянный `output/document.pdf`. Он проверяет build-report,
postflight, SHA-256 и актуальность исходников; если после Draft изменился хотя
бы один входной файл, публикация fail-closed потребует новую сборку. Экспорт
Draft разрешён с предупреждением, а `archive` никогда не перезаписывает уже
созданный снимок.

### Установка зависимостей

Команда `install` проверяет окружение и предлагает установить отсутствующий
Pandoc через точный идентификатор пакета WinGet:

```powershell
winget install --id JohnMacFarlane.Pandoc --exact --source winget
```

Для запуска без вопроса подтверждения можно использовать
`.\AutoNormoKontrol.cmd install --yes`. Соглашения источника и пакета принимаются
только после этого явного действия пользователя.

TeX Live на момент проверки отсутствует в каталоге WinGet. Его следует ставить
официальным `install-tl-windows.exe` с
<https://tug.org/texlive/windows.html>. Устанавливать MiKTeX вместо TeX Live
скрипт не будет, поскольку это изменит проверенное окружение сборки.

Git и VS Code считаются необязательными инструментами и автоматически не
устанавливаются.

## Сборка и проверка

Черновой и строгий режимы запускаются только внутри конкретной работы:

```powershell
.\AutoNormoKontrol.cmd draft
.\AutoNormoKontrol.cmd strict
```

Для пишущего агента используйте компактный режим. Он выводит только результат
либо код ошибки, исходный файл, строку и краткое сообщение:

```powershell
.\AutoNormoKontrol.cmd draft --quiet
```

Полный технический вывод сохраняется как внутренний диагностический артефакт,
но обычная команда не предлагает агенту читать его.

`Strict` работает fail-closed. Он намеренно не проходит, пока не указаны
дисциплина, выданное кафедрой задание, персональные реквизиты, внешние решения и
проверенный семантический аудит, привязанный к хэшу текущего содержания.

Полная проверка программы запускается отдельно из корня движка:

```powershell
.\AutoNormoKontrol.cmd check --fast
.\AutoNormoKontrol.cmd check
```

`check --fast` предназначен для промежуточного цикла разработчика и не запускает
дорогой disposable-workspace lifecycle. Обычный `check` использует одноразовые
fixtures/workspaces, остаётся обязательным перед завершением изменения движка и
не собирает документ из корня движка.

Технические результаты сборки внутри текущего workspace:

- `build/coursework.pdf` — проверенный, но disposable PDF сборки;
- `build/coursework.tex` — промежуточный LaTeX для диагностики;
- `build/build-report.json` — снимок сборки и использованные ассеты;
- `build/asset-report.json` — хэши manifest, исходников и PDF-ассетов;
- `build/document-snapshot.json` — полный набор файлов, связанный с
  `content-hash`;
- `build/compliance-report.json` — результаты физической проверки PDF.

Пользовательский результат после `export` — `output/document.pdf`.

## Формальный контракт профиля

`profiles/catalog.json` явно регистрирует доверенные пакеты, которые разрешено
показывать и выбирать. `profiles/active-profile.txt` выбирает среди них профиль
по умолчанию для команды `new` и тестов движка. Конкретная работа закрепляет
точный путь и digest профиля в своём `project.yaml`; сборка не подменяет этот
выбор указателем из корня. Нерегистрируемые каталоги не обнаруживаются и не
исполняются автоматически.
Manifest профиля задаёт стабильный ID и тип документа, стартовый набор глав,
нормативный реестр, шаблоны журналов приёмки, canonical review inventory,
шаблон, фильтры, стили, capabilities, выходы и отчёты. Формат описан точной схемой
`schemas/profile-v1.schema.json`.

Профиль задаёт стартовый список глав для `new`. После создания работы
единственным источником её точного порядка является
`project.yaml: document.content`. Сборка без `project.yaml` не поддерживается.

Manifest записан в JSON-совместимом подмножестве YAML 1.2. Это позволяет
PowerShell 5.1 проверять его без стороннего YAML-модуля. Неизвестное поле,
неподдерживаемая версия, отсутствующий источник или путь за пределы репозитория
останавливают сборку. Указатель обязан содержать ровно один существующий путь
вида `profiles/<profile-id>/profile.yaml`. Поиск «первого найденного профиля» и
неявный fallback намеренно отсутствуют.

Каталог профиля хранит неизменяемую реализацию правил: canonical inventory,
`requirements.json`, review templates, системную инструкцию, Lua-фильтры,
LaTeX-шаблон, стиль и PDF-postflight. Каталог `Workspaces/<name>/` хранит данные конкретной
работы: `content/`, `metadata.yaml`, `bibliography.bib`, `assets/`,
`format-spec.yaml` и активные `compliance/semantic-review.yaml` и
`compliance/external-acceptance.yaml`. Последние нельзя переиспользовать как
готовое доказательство для другой работы.
В корне движка эти данные и evidence запрещены.

`profile_digest` связывает manifest с нормативным реестром, review inventory,
системной инструкцией, шаблоном, стилями, Lua-фильтрами и PDF-postflight. ID и
digest записываются в `build/build-report.json` и отчёт трассировки. Это не
заменяет `content-hash`: первый идентифицирует реализацию правил, второй —
конкретный проверяемый снимок содержания и ассетов.

Публичные `list-profiles` и `new --profile <id>` уже выбирают пакет при создании
работы. Смена профиля существующего workspace и второй реальный тип документа
остаются следующими этапами roadmap; диагностические параметры внутренних
скриптов не являются механизмом миграции.

## Воспроизводимые ассеты

Каталог `assets/` содержит только версионируемые исходники конкретной работы:

- `assets/data/` — CSV с входными данными;
- `assets/plots/` — доверенные TeX/pgfplots-исходники графиков;
- `assets/images/` — статические пользовательские изображения;
- `assets/manifest.json` — белый список ассетов и их происхождение.

`build/assets/` является полностью производным каталогом. Его PDF нельзя
редактировать вручную или считать единственным исходником. Обычная команда
`draft` сначала проверяет manifest, затем собирает объявленные графики без
`shell-escape`, вычисляет хэши и только после этого запускает Pandoc.

Минимальный сценарий создания графика:

1. Добавь CSV как `assets/data/<id>.csv`.
2. Добавь читающий этот CSV pgfplots-исходник `assets/plots/<id>.tex`.
3. Зарегистрируй оба входа и `build/assets/<id>.pdf` в
   `assets/manifest.json`.
4. Выполни `.\AutoNormoKontrol.cmd draft` и проверь секцию `used_assets` в
   `build/build-report.json`.

Изменение manifest, CSV, TeX-исходника или готового PDF меняет
`content-hash`. Поэтому прежний `semantic-review.status: pass`, если он был
выдан для другого снимка, больше не может открыть Strict-сборку.

## Как отслеживается каждый пункт СТО

Источник трассировки задаётся полем `compliance.requirements` активного manifest.
Для текущего профиля это
`profiles/susu-hsem-ceit-coursework-v1/compliance/requirements.json`. Для каждого
пункта там зафиксированы применимость, механизм закрытия, статус, реализация и
тест. Exact-set всех 172 ID хранится рядом в
`canonical-requirement-ids.json`, а не зашивается в общий движок.

В реальном обработчике и тесте размещается тот же маркер, например:

```text
STO-8.5.3
```

`scripts/check-coverage.ps1` завершает сборку ошибкой, если программный пункт не
имеет маркера одновременно в реализации и тесте, смысловой пункт отсутствует в
системной инструкции/аудите, а внешний — в журнале внешних подтверждений. Сам
реестр не может доказать собственное покрытие.

Воспроизводимый отчёт с точными ссылками `file:line` создаётся автоматически
при проверке движка. Внутренний скрипт формирует
`build/sto-traceability.json` и `build/sto-traceability.md`, но не является
публичной пользовательской командой. Отчёт не заменяет coverage gate и не
доказывает фактическое принятие внешнего пункта.

Основные файлы контроля:

- `AGENTS.md` — автоматически обнаруживаемый контракт работы агента в
  репозитории;
- `profiles/active-profile.txt` — явный профиль по умолчанию для `new` и
  тестов движка;
- `profiles/catalog.json` — явный список доверенных профилей, доступных CLI;
- `Workspaces/<работа>/project.yaml` — закреплённые manifest и digest профиля
  конкретной работы;
- поле `compliance.system_prompt` закреплённого manifest — обязательная
  системная инструкция для ИИ этой работы;
- `Workspaces/<работа>/guide/profile-system-prompt.md` — её начальная локальная
  копия для агента; пользователь может адаптировать её для конкретной работы;
- `Workspaces/<работа>/compliance/semantic-review.yaml` — смысловой аудит с цитатами/доказательствами
  и хэшем содержания;
- `Workspaces/<работа>/compliance/external-acceptance.yaml` — решения кафедры и другие внешние факты;
- поле `compliance.research_notes` закреплённого manifest — результат поиска
  публичных локальных правил;
- `sources/susu/sto-21-2008/original/CTOsusu21-2008.pdf` — канонический
  оригинал 56-страничного СТО;
- `sources/susu/sto-21-2008/derived/standard.layout.txt` — воспроизводимая
  текстовая копия для нормативной трассировки;
- `sources/susu/sto-21-2008/manifest.json` — provenance, SHA-256 оригинала,
  производных данных и приложений.

## Что редактировать в workspace

- `metadata.yaml` — тема, дисциплина, студент, группа, руководитель,
  нормоконтролер, код и реквизиты задания;
- `content/*.md` — только содержание;
- `bibliography.bib` — только реально использованные источники;
- `format-spec.yaml` — активные решения профиля и их происхождение.

Файлы документа существуют только внутри `Workspaces/<работа>/`. Корень движка
не является workspace и не поддерживает старую корневую раскладку.

Базовый вариант пункта 8.1.3 — Times New Roman 14 пт, одинарный интервал и
абзацный отступ 0,7 см. Полуторный интервал и 1,5 см допустимы только после
зафиксированного решения кафедры.

## Ограниченная нотация

Файлы `content/*.md` не имеют права управлять вёрсткой. Raw TeX/HTML, ручные
номера, `\vspace`, `\fontsize`, `\newpage`, произвольные классы и атрибуты
отклоняются до LaTeX.

Рисунок:

```markdown
Данные представлены на рисунке [@fig:extraction].

![Название графика](build/assets/example-plot.pdf){#fig:example-plot kind=graph x-axis="Ось X" y-axis="Ось Y" width=80%}
```

Формула с расшифровкой:

```markdown
Значение определяется по формуле [@eq:quality].

::: {#eq:quality .equation symbols="Q,N_all"}
$$
Q = 1 / N_{\mathrm{all}},
$$
:::

::: {.equation-where for="eq:quality"}
- $Q$ — значение показателя
- $N_{\mathrm{all}}$ — общее количество элементов
:::
```

Приложение:

```markdown
Данные приведены в приложении [@app:data].

::: {#app:data .susu-appendix letter="А"
      title="Исходные данные" kind="информационное"}
Текст приложения.
:::
```

Если новое требование невозможно выразить этой нотацией, меняется доверенный
фильтр/стиль, добавляется тест и маркер соответствующего пункта СТО. Обходной
визуальный хак в содержании не допускается.
