# Работа со статьёй

Полностью прочитай:

1. `AGENTS.md`;
2. `guide/profile-system-prompt.md`;
3. `metadata.yaml`;
4. `content/article.md`.

Не применяй правила курсовой работы автоматически: эта рабочая область
создана профилем научной статьи.

После содержательных изменений выполни:

AutoNormoKontrol.cmd draft

Не редактируй сгенерированные файлы в `build/**` и `output/**`.
